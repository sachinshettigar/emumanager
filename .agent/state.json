{
  "$schema": "./state.schema.json",
  "project": "emumanager",
  "updated": "2026-09-05",
  "currentMilestone": "M2",
  "lastValidatedCommit": "aaf4249",
  "note": "Toolchains: rustc 1.98.1, pnpm 10.0.0, just 1.58 (brew), java 21 (system JDK). Tasks 0001-0008 done, 0009 in review -- blocked on GitHub Actions billing, not code (see prior notes/journal); deprioritized. M1 functionally complete (0010-0013 done) but stays in_progress -- two deliberately-deferred DoD boxes, see MILESTONES.md. M2 scoped into 4 tasks (0014-0017, .agent/tasks/), same granularity as M1; M2 flipped to in_progress (M0/M1/M2 now one contiguous in_progress run, still satisfies progress-check.mjs). Task 0014 done: two new pure-parser modules in emu-android -- devices::parse reads Android's own hardware-profile XML files (devices.xml, nexus.xml, wear.xml, tv.xml, automotive.xml, desktop.xml -- all shipped inside cmdline-tools' sdklib.core.jar) directly, since avdmanager list device's plain-text output (captured for real: 15 devices, id/Name/OEM/Tag only) doesn't carry the screen/RAM/sensor data DeviceProfile needs; sysimg::parse reads Google's per-tag sys-img2-3.xml manifests (confirmed by a fresh real fetch: system images are NOT in repository2-3.xml at all -- one manifest per Google tag family, 6 real curl-verified URLs in sysimg::MANIFEST_URLS). Real fixtures captured fresh 2026-09-05 (real cmdline-tools download + real sys-img2-3.xml fetches), trimmed to verbatim real bytes, same trim policy as task 0010's repository2-3.xml fixture. Two real data quirks found and documented in task 0014's Notes: (1) RAM ships in three different units across real files (GiB/nexus, MiB/wear, KiB/automotive) -- all three exercised by a real fixture; (2) an extension-level system-image package (android-34-ext12;...) doesn't round-trip through the existing ImageCoord::from_str (task 0002) -- reused that existing rejection as the skip signal instead of writing separate ext-level detection. Only wear.xml/tv.xml/automotive.xml carry an explicit <d:tag-id>; devices.xml/nexus.xml/desktop.xml don't, so form factor for those three is inferred from id/name (documented UI heuristic, not cited SDK behavior -- called out as such in the module doc). xr.xml (glasses/XR devices) exists in the real jar too but is explicitly out of scope -- no XR FormFactor variant, not in docs/spec.md. 19 new unit tests, all fixture-driven, no network, no process spawn (locating/extracting these files from an installed SDK at runtime is task 0015's job, alongside ensure_image/create which need the same install-dir knowledge). just validate green; no IPC surface touched so bindings.ts regenerated as a no-op diff. Also this session: rebuilt + relaunched the app first (confirmed the M1/task-0013 build runs with no errors) before starting M2 scoping work. Deferred, unchanged from before: .sqlx/ offline cache + query! macros (M3); gitleaks/lychee/actionlint/typos CI installers (`just setup`); e2e suite (M2+); branch protection (human action); Downloader pause/resume/cancel/queueing; sdkmanager --list system-image text parsing (superseded by task 0014's direct-XML approach -- the plain-text sdkmanager --list output is not needed now that the real manifests are parsed directly). Next: task 0015 -- AndroidProvider::ensure_image (download+install a system image) + create (avdmanager create avd), the first real Provider implementation.",
  "coverageGate": {
    "rust": null,
    "web": null
  },
  "milestones": {
    "M0": {
      "title": "Skeleton & gate",
      "status": "in_progress"
    },
    "M1": {
      "title": "Toolchain manager: SDK from zero",
      "status": "in_progress"
    },
    "M2": {
      "title": "Create & launch one emulator end-to-end",
      "status": "in_progress"
    },
    "M3": {
      "title": "Registry & reliable tracking",
      "status": "todo"
    },
    "M4": {
      "title": "Profiles: export / import / recreate",
      "status": "todo"
    },
    "M5": {
      "title": "Host readiness & elevated helper",
      "status": "todo"
    },
    "M6": {
      "title": "Cross-platform hardening & packaging",
      "status": "todo"
    },
    "M7": {
      "title": "Feature-complete v1.0",
      "status": "todo"
    }
  },
  "tasks": [
    {
      "id": "0001",
      "title": "Cargo workspace skeleton",
      "milestone": "M0",
      "status": "done",
      "validate": "cargo build --workspace"
    },
    {
      "id": "0002",
      "title": "emu-core ports and models (no impls)",
      "milestone": "M0",
      "status": "done",
      "validate": "cargo test -p emu-core --all-features"
    },
    {
      "id": "0003",
      "title": "Vite + React + TS strict app shell with 4 routes",
      "milestone": "M0",
      "status": "done",
      "validate": "pnpm -C . run typecheck && pnpm -C . run test"
    },
    {
      "id": "0004",
      "title": "tauri-specta wired: ping command + generated bindings",
      "milestone": "M0",
      "status": "done",
      "validate": "just bindings && git diff --exit-code src/lib/bindings.ts"
    },
    {
      "id": "0005",
      "title": "sqlx + initial migration + committed .sqlx offline data",
      "milestone": "M0",
      "status": "done",
      "validate": "SQLX_OFFLINE=true cargo test -p emu-core registry_open"
    },
    {
      "id": "0006",
      "title": "justfile + package.json script mirror + scripts/",
      "milestone": "M0",
      "status": "done",
      "validate": "just --summary"
    },
    {
      "id": "0007",
      "title": "just validate wired end to end (all static tools)",
      "milestone": "M0",
      "status": "done",
      "validate": "just validate"
    },
    {
      "id": "0008",
      "title": "lefthook hooks (pre-commit regen+fmt, pre-push validate)",
      "milestone": "M0",
      "status": "done",
      "validate": "pnpm exec lefthook run pre-commit"
    },
    {
      "id": "0009",
      "title": "CI: ci.yml matrix + schema.yml + emu-core-no-tauri check",
      "milestone": "M0",
      "status": "review",
      "validate": "actionlint .github/workflows/*.yml"
    },
    {
      "id": "0010",
      "title": "emu-android: SDK component catalog from Google's repository XML",
      "milestone": "M1",
      "status": "done",
      "validate": "cargo test -p emu-core -p emu-android --all-features"
    },
    {
      "id": "0011",
      "title": "src-tauri: real ProcessRunner + Downloader + Clock + Fs port impls",
      "milestone": "M1",
      "status": "done",
      "validate": "cargo test -p emumanager --all-features"
    },
    {
      "id": "0012",
      "title": "emu-core: toolchain manager — InstalledState scan + bootstrap sdkmanager",
      "milestone": "M1",
      "status": "done",
      "validate": "cargo test -p emu-core --all-features"
    },
    {
      "id": "0013",
      "title": "IPC + Dependencies screen wired to real toolchain state",
      "milestone": "M1",
      "status": "done",
      "validate": "just bindings && git diff --exit-code src/lib/bindings.ts"
    },
    {
      "id": "0014",
      "title": "emu-android: real device catalog + system-image catalog (parsers, no process spawn)",
      "milestone": "M2",
      "status": "done",
      "validate": "cargo test -p emu-core -p emu-android --all-features"
    },
    {
      "id": "0015",
      "title": "emu-android: AndroidProvider — ensure_image + create",
      "milestone": "M2",
      "status": "todo",
      "validate": "cargo test -p emu-core -p emu-android --all-features"
    },
    {
      "id": "0016",
      "title": "emu-android: AndroidProvider — launch + stop",
      "milestone": "M2",
      "status": "todo",
      "validate": "cargo test -p emu-core -p emu-android --all-features"
    },
    {
      "id": "0017",
      "title": "IPC + Create wizard screen + Dashboard wired to real Provider",
      "milestone": "M2",
      "status": "todo",
      "validate": "just bindings && git diff --exit-code src/lib/bindings.ts"
    }
  ]
}
