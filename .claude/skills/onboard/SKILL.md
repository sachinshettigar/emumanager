---
name: onboard
description: Load EmuManager project context at the start of a session. Use when starting fresh, resuming after a break, or switching into this repo.
---

Follow `docs/playbooks/bootstrap-new-harness.md` and use the checklist in `docs/prompts/onboard.md`.

Read `AGENTS.md`, `docs/spec.md`, `docs/architecture.md`, the newest `.agent/journal/` entry, then
run `just progress` and open `.agent/state.json`. Report the current milestone, next `todo` task,
and any gotchas before implementing anything.
