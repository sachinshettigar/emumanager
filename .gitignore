# Rust
/target
**/*.rs.bk

# Node
node_modules
pnpm-debug.log*
.pnpm-store
*.tsbuildinfo

# Vite / frontend build (keep the M0 placeholder until task 0003 wires the real build)
dist/*
!dist/index.html
dist-ssr
*.local

# Tauri
src-tauri/target
src-tauri/gen

# Test / coverage
coverage
lcov.info
.nyc_output
target/nextest

# sqlx: keep .sqlx/ (offline metadata is committed); ignore local DB
*.sqlite
*.sqlite-*
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json

# App data written during local runs
/.scratch
/tmp
.claude/
